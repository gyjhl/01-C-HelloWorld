#include<iostream>
#include<string>
#include<ctime>
using namespace std;


/*
//结构体指针

//定义一个学生的结构体
struct Student
{
	string name;	//成员姓名
	int age;	//成员年龄
	int score;	//成员分数
};

int main() {

	//1, 创建一个结构体变量
	struct Student s = { "张三", 18, 100 };

	//2, 通过指针来访问结构体变量
	struct Student * p = &s;

	//3, 通过指针访问结构体变量中的数据
	//通过结构体指针访问结构体中的属性, 需要利用'->'
	cout << "姓名: " << p->name << " 年龄: " << p->age << " 分数: " << p->score << endl;


	system("pause");
	return 0;
}
*/