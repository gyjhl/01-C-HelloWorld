#include<iostream>
#include<string>
#include<ctime>
using namespace std;
/*
int main() {

	int a = 10; int b = 10;

	//const修饰指针的三种情况:
	//1, const修饰指针     ----常量指针
	//特点: 指针的指向(的变量)可以修改, 但是指针指向的值不能修改
	//const int *p = &a;

	//判断哪个语句是对的, 哪个是错的
	//const int *p = &a;
	//1,
	//*p = 20;(错误: 指针指向的值不可以改)
	//2,
	//p = &b;(正确: 指针的指向可以修改)




	//2, const修饰常量     ----指针常量
	// int * const p = &a;
	//const后面跟的是常量
	//特点: 指针的指向不可以改, 但是指针指向的值可以改

	//判断哪个语句是对的, 哪个是错的
	// int * const p = &a;
	//1,
	//*p = 20;(正确: 指针指向的值可以改)
	//2,
	//p = &b;(错误: 指针的指向不可以修改)

	//3, const既修饰指针, 又修饰常量
	//const int * const p = &a;
	//特点: 指针的指向, 和指针指向的值都不可以改
	//判断哪个语句是对的, 哪个是错的
	// const int * const p = &a;
	//1,
	//*p = 20;(错误: 指针指向的值不可以改)
	//2,
	//p = &b;(错误: 指针的指向不可以修改)


	system("pause");
	return 0;
}
*/