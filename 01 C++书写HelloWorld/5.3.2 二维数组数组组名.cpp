#include<iostream>
#include<string>
#include<ctime>
using namespace std;
/*
int main() {


	//二维数组组名称的用途

	//1, 可以统计整个数组在内存中的长度
	int arr[2][3] =
	{
		{1,2,3},
		{4,5,6}
	};

	cout <<"二维数组占用内存空间为:"<< sizeof(arr) << endl;
	cout <<"二维数组第一行占用内存空间为:" << sizeof(arr[0]) << endl;
	cout <<"二维数组第一个元素占用内存空间为:" << sizeof(arr[0][0]) << endl;
	cout <<"二维数组行数为:" << sizeof(arr) / sizeof(arr[0]) << endl;
	cout <<"二维数组列数为:" << sizeof(arr[0]) / sizeof(arr[0][0]) << endl;
	//cout <<"数组中元素个数为:" << sizeof(arr)/ sizeof(arr[0]) << endl;

	//2, 可以查看二维数组在内存中的首地址
	cout <<"二维数组首地址为:" << (int)arr << endl;
	cout <<"二维数组第一行的地址址为:" << (int)arr[0]<< endl;
	cout <<"二维数组第二行的地址址为:" << (int)arr[1] << endl;
	cout <<"二维数组第一个元素的地址址为:" << (int)&arr[0][0] << endl;
	cout <<"二维数组第一个元素的地址址为:" << (int)&arr[0][1] << endl;
	system("pause");
	return 0;
}
*/